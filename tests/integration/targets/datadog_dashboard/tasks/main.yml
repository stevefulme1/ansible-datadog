---
# Integration tests for stevefulme1.datadog.datadog_dashboard
# These tests require valid credentials configured in integration_config.yml

- name: Create dashboard (check_mode)
  stevefulme1.datadog.datadog_dashboard:
    host: "{{ datadog_host }}"
    api_key: "{{ datadog_api_key }}"
    app_key: "{{ datadog_app_key | default(omit) }}"
    state: present
    title: "ansible-test-{{ resource_prefix }}"
    layout_type: ordered
    widgets:
      - definition:
          type: timeseries
          requests:
            - q: "avg:system.cpu.user{*}"
    validate_certs: "{{ validate_certs | default(true) }}"
  check_mode: true
  register: check_result

- name: Verify check_mode reports changed
  ansible.builtin.assert:
    that:
      - check_result is changed
      - check_result.diff is defined

- name: Create dashboard
  stevefulme1.datadog.datadog_dashboard:
    host: "{{ datadog_host }}"
    api_key: "{{ datadog_api_key }}"
    app_key: "{{ datadog_app_key | default(omit) }}"
    state: present
    title: "ansible-test-{{ resource_prefix }}"
    layout_type: ordered
    widgets:
      - definition:
          type: timeseries
          requests:
            - q: "avg:system.cpu.user{*}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: create_result

- name: Verify dashboard was created
  ansible.builtin.assert:
    that:
      - create_result is changed
      - create_result.dashboard is defined
      - create_result.dashboard.id is defined

- name: Create dashboard again (idempotency)
  stevefulme1.datadog.datadog_dashboard:
    host: "{{ datadog_host }}"
    api_key: "{{ datadog_api_key }}"
    app_key: "{{ datadog_app_key | default(omit) }}"
    state: present
    title: "ansible-test-{{ resource_prefix }}"
    layout_type: ordered
    widgets:
      - definition:
          type: timeseries
          requests:
            - q: "avg:system.cpu.user{*}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: idem_result

- name: Verify idempotent create returns changed=false
  ansible.builtin.assert:
    that:
      - idem_result is not changed

- name: Delete dashboard (check_mode)
  stevefulme1.datadog.datadog_dashboard:
    host: "{{ datadog_host }}"
    api_key: "{{ datadog_api_key }}"
    app_key: "{{ datadog_app_key | default(omit) }}"
    state: absent
    dashboard_id: "{{ create_result.dashboard.id }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  check_mode: true
  register: delete_check

- name: Verify check_mode delete reports changed
  ansible.builtin.assert:
    that:
      - delete_check is changed

- name: Delete dashboard
  stevefulme1.datadog.datadog_dashboard:
    host: "{{ datadog_host }}"
    api_key: "{{ datadog_api_key }}"
    app_key: "{{ datadog_app_key | default(omit) }}"
    state: absent
    dashboard_id: "{{ create_result.dashboard.id }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: delete_result

- name: Verify dashboard was deleted
  ansible.builtin.assert:
    that:
      - delete_result is changed

- name: Delete dashboard again (idempotent)
  stevefulme1.datadog.datadog_dashboard:
    host: "{{ datadog_host }}"
    api_key: "{{ datadog_api_key }}"
    app_key: "{{ datadog_app_key | default(omit) }}"
    state: absent
    dashboard_id: "{{ create_result.dashboard.id }}"
    validate_certs: "{{ validate_certs | default(true) }}"
  register: delete_idem

- name: Verify idempotent delete returns changed=false
  ansible.builtin.assert:
    that:
      - delete_idem is not changed
