---
argument_specs:
  main:
    short_description: "Configure APM tracing and service maps"
    options:
      datadog_apm_setup_host:
        description: Target host address
        type: str
        required: true
      datadog_apm_setup_username:
        description: Authentication username
        type: str
      datadog_apm_setup_password:
        description: Authentication password
        type: str
        no_log: true
      datadog_apm_setup_api_key:
        description: API key for authentication
        type: str
        no_log: true
      datadog_apm_setup_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      datadog_apm_setup_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
