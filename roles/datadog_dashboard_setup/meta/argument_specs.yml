---
argument_specs:
  main:
    short_description: "Create service dashboards with standard widgets"
    options:
      datadog_dashboard_setup_host:
        description: Target host address
        type: str
        required: true
      datadog_dashboard_setup_username:
        description: Authentication username
        type: str
      datadog_dashboard_setup_password:
        description: Authentication password
        type: str
      datadog_dashboard_setup_api_key:
        description: API key for authentication
        type: str
      datadog_dashboard_setup_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      datadog_dashboard_setup_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
