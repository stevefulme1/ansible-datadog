---
argument_specs:
  main:
    short_description: "Deploy and configure Datadog Agent across hosts"
    options:
      datadog_agent_deploy_host:
        description: Target host address
        type: str
        required: true
      datadog_agent_deploy_username:
        description: Authentication username
        type: str
      datadog_agent_deploy_password:
        description: Authentication password
        type: str
        no_log: true
      datadog_agent_deploy_api_key:
        description: API key for authentication
        type: str
        no_log: true
      datadog_agent_deploy_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      datadog_agent_deploy_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
